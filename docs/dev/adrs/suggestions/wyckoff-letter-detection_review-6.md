# Review 6: Automatic Wyckoff Position Detection

**No findings. Ready to commit.**

The review-5 finding was addressed: empty-letter sites now have explicit
derived descriptor semantics, CIF output, and calculator handling for
the no-`WyckoffPosition` case.

Checks skipped per `AGENTS.md` review-shortcut rules: static ADR/source
review only; no tests, `pixi run fix`, `pixi run check`, or other
verification commands were run.
