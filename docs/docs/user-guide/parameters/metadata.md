[easydiffractionCIF][0]{:.label-cif}

# `_metadata`

EasyDiffraction EdSTAR reference for the `_metadata` category.
These entries provide stable anchors for runtime parameter links.

### `_metadata.created` { #metadata-created }

EdSTAR key `_metadata.created`.

### `_metadata.description` { #metadata-description }

EdSTAR key `_metadata.description`.

### `_metadata.last_modified` { #metadata-last-modified }

EdSTAR key `_metadata.last_modified`.

### `_metadata.name` { #metadata-name }

EdSTAR key `_metadata.name`.

### `_metadata.timestamp` { #metadata-timestamp }

EdSTAR key `_metadata.timestamp`.

### `_metadata.title` { #metadata-title }

EdSTAR key `_metadata.title`.
<!-- prettier-ignore-start -->
[0]: #
[1]: https://www.iucr.org/resources/cif/dictionaries/browse/cif_core
[2]: https://www.iucr.org/resources/cif/dictionaries/browse/cif_pd
<!-- prettier-ignore-end -->
