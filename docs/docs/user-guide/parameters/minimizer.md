[easydiffractionCIF][0]{:.label-cif}

# `_minimizer`

EasyDiffraction EdSTAR reference for the `_minimizer` category. These
entries provide stable anchors for runtime parameter links.

### `_minimizer.burn_in_steps` { #minimizer-burn-in-steps }

EdSTAR key `_minimizer.burn_in_steps`.

### `_minimizer.initialization_method` { #minimizer-initialization-method }

EdSTAR key `_minimizer.initialization_method`.

### `_minimizer.max_iterations` { #minimizer-max-iterations }

EdSTAR key `_minimizer.max_iterations`.

### `_minimizer.parallel_workers` { #minimizer-parallel-workers }

EdSTAR key `_minimizer.parallel_workers`.

### `_minimizer.population_size` { #minimizer-population-size }

EdSTAR key `_minimizer.population_size`.

### `_minimizer.proposal_moves` { #minimizer-proposal-moves }

EdSTAR key `_minimizer.proposal_moves`.

### `_minimizer.random_seed` { #minimizer-random-seed }

EdSTAR key `_minimizer.random_seed`.

### `_minimizer.sampling_steps` { #minimizer-sampling-steps }

EdSTAR key `_minimizer.sampling_steps`.

### `_minimizer.thinning_interval` { #minimizer-thinning-interval }

EdSTAR key `_minimizer.thinning_interval`.

### `_minimizer.type` { #minimizer-type }

EdSTAR key `_minimizer.type`.

<!-- prettier-ignore-start -->
[0]: #
[1]: https://www.iucr.org/resources/cif/dictionaries/browse/cif_core
[2]: https://www.iucr.org/resources/cif/dictionaries/browse/cif_pd
<!-- prettier-ignore-end -->
