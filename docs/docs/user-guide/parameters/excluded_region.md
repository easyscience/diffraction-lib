[easydiffractionCIF][0]{:.label-cif}

# `_excluded_region`

EasyDiffraction EdSTAR reference for the `_excluded_region` category.
These entries provide stable anchors for runtime parameter links.

### `_excluded_region.end` { #excluded-region-end }

EdSTAR key `_excluded_region.end`.

### `_excluded_region.id` { #excluded-region-id }

EdSTAR key `_excluded_region.id`.

### `_excluded_region.start` { #excluded-region-start }

EdSTAR key `_excluded_region.start`.

<!-- prettier-ignore-start -->
[0]: #
[1]: https://www.iucr.org/resources/cif/dictionaries/browse/cif_core
[2]: https://www.iucr.org/resources/cif/dictionaries/browse/cif_pd
<!-- prettier-ignore-end -->
