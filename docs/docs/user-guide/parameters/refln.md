[easydiffractionCIF][0]{:.label-cif}

# `_refln`

EasyDiffraction EdSTAR reference for the `_refln` category. These
entries provide stable anchors for runtime parameter links.

### `_refln.d_spacing` { #refln-d-spacing }

EdSTAR key `_refln.d_spacing`.

### `_refln.f_calc` { #refln-f-calc }

EdSTAR key `_refln.f_calc`.

### `_refln.f_squared_calc` { #refln-f-squared-calc }

EdSTAR key `_refln.f_squared_calc`.

### `_refln.id` { #refln-id }

EdSTAR key `_refln.id`.

### `_refln.index_h` { #refln-index-h }

EdSTAR key `_refln.index_h`.

### `_refln.index_k` { #refln-index-k }

EdSTAR key `_refln.index_k`.

### `_refln.index_l` { #refln-index-l }

EdSTAR key `_refln.index_l`.

### `_refln.intensity_calc` { #refln-intensity-calc }

EdSTAR key `_refln.intensity_calc`.

### `_refln.intensity_meas` { #refln-intensity-meas }

EdSTAR key `_refln.intensity_meas`.

### `_refln.intensity_meas_su` { #refln-intensity-meas-su }

EdSTAR key `_refln.intensity_meas_su`.

### `_refln.sin_theta_over_lambda` { #refln-sin-theta-over-lambda }

EdSTAR key `_refln.sin_theta_over_lambda`.

### `_refln.sin_theta_over_lambda_range_max` { #refln-sin-theta-over-lambda-range-max }

EdSTAR key `_refln.sin_theta_over_lambda_range_max`.

### `_refln.sin_theta_over_lambda_range_min` { #refln-sin-theta-over-lambda-range-min }

EdSTAR key `_refln.sin_theta_over_lambda_range_min`.

### `_refln.structure_id` { #refln-structure-id }

EdSTAR key `_refln.structure_id`.

### `_refln.time_of_flight` { #refln-time-of-flight }

EdSTAR key `_refln.time_of_flight`.

### `_refln.two_theta` { #refln-two-theta }

EdSTAR key `_refln.two_theta`.

### `_refln.wavelength` { #refln-wavelength }

EdSTAR key `_refln.wavelength`.

<!-- prettier-ignore-start -->
[0]: #
[1]: https://www.iucr.org/resources/cif/dictionaries/browse/cif_core
[2]: https://www.iucr.org/resources/cif/dictionaries/browse/cif_pd
<!-- prettier-ignore-end -->
